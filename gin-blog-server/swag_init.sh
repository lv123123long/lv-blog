swag init -g ./cmd/main.go

# swagger是一个用于描述、生成和可视化RESTful API的工具集
# 初始化项目，生成swagger文档
# init 是子命令，初始化项目
# -g 指定go代码文件
# Swagger 注释规范的代码，并生成相应的 Swagger 文档文件（通常是 swagger.json 或 swagger.yaml），这些文件可以被 Swagger UI 或其他 Swagger 兼容的工具使用，以可视化你的 API。